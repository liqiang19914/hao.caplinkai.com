# macOS
.DS_Store
__MACOSX/

# 超过 GitHub 100MB 单文件限制的原始归档（本地与服务器另行保留）
归档.zip

# 运行时生成的目录与文件（以服务器为准，不进 Git）
# 注意：必须锚定根目录，否则会误伤框架内的 Cache/ 目录和各子目录的 index.html 防访问占位文件
/cache/
/index.html

# IDE
.idea/
.vscode/

# 运维/数据体检工作区（不进仓库）
_ops/
